# Output_data

gtEditor writes reviewed data here. For each input dataset, `Save` writes to `saved/` and `Discard` writes to `discarded/`:

```text
Output_data/
  <dataset-name>/
    saved/
      image/
      json/
    discarded/
      image/
      json/
```

Generated output contents are intentionally ignored by git.
