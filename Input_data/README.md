# Input_data

Place one or more review datasets here. Each dataset folder must contain matching `image/` and `json/` children:

```text
Input_data/
  <dataset-name>/
    image/
      <table_id>.png
    json/
      <table_id>.json
```

Dataset contents are intentionally ignored by git.
